# ============================================================================
# Completion System Configuration
# ============================================================================

# Add custom functions and completions directories to fpath
fpath=(
  "$XDG_CONFIG_HOME/zsh/functions"
  "$XDG_CONFIG_HOME/zsh/completions"
  "${fpath[@]}"
)

# Autoload custom functions
autoload -Uz $XDG_CONFIG_HOME/zsh/functions/*(:t)

# Initialize completion system
autoload -Uz compinit
compinit

# Load dynamic tool completions
source <(jj util completion zsh)
znap fpath _rv 'rv shell completions zsh'

# bun completions
[ -s "/Users/martinemde/.bun/_bun" ] && source "/Users/martinemde/.bun/_bun"

# vi: ft=zsh
